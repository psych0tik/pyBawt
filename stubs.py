"""A loader interface to the stubs inside pyBawt's test harness, to make them
available to the test suite"""

# TODO

class message(dict):
    """for now just a thing that you can load more things into so much TODO"""
   #def __getattribute__(self, key):
   #    return self[key]
   #def __setattribute__(self, key, value):
   #    self[key] = value
    pass
